{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "inputs": [ "src/**" ],
      "outputs": [ "dist/**" ]
    },
    "@supercord/core#build": {
      "inputs": [ "{ipc,log}/**" ],
      "outputs": [ "dist/**" ]
    },
    "lint": {
      "outputs": [ ]
    }
  }
}
